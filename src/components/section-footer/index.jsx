import React, { memo } from 'react'
import { FooterWrapper } from './style'

const SectionFooter = memo(() => {
  return (
    <FooterWrapper>SectionFooter</FooterWrapper>
  )
})

export default SectionFooter